# API Configuration
# Read at container/server start (not baked into the client bundle at build time),
# so the same built image can point at a different backend per deployment.
# For development: http://localhost:8080/api/v1
# For production: https://api.yourdomain.com/api/v1
API_URL=http://localhost:8080/api/v1
